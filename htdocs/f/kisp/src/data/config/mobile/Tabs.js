/**
* Tabs 模块的默认配置
* @name Tabs.config
*/
define('Tabs.config', /**@lends Tabs.config*/ {
    /**
    * 要监听的事件名。
    * 当指定为 'touch' 时，会调用 $(container).touch()进行绑定。 
    */
    eventName: 'touch',

});

